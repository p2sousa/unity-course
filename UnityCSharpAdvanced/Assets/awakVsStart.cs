using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class awakVsStart : MonoBehaviour
{

    void Awake()
    {
        Debug.Log("Awak Executada!");
    }

    // Start is called before the first frame update
    void Start()
    {        
        Debug.Log("Start Executada!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
